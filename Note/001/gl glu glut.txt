    gl库是核心库，glu是实用库，glut是实用工具库。

    gl是核心，glu是对gl的部分封装，glut是OpenGL的跨平台工具库，gl中包含了最
基本的3D函数，而glu似乎对gl的辅助，如果算数好，不用glu的情况下，也是可以做出
同样的效果。
    glut是基本的窗口界面，是独立于gl和glu的，如果不喜欢用glut可以用MFC和Win32
窗口等代替，但是glut是跨平台的，这就保证了我们编出的程序是跨平台的，如果用MFC
或者Win32只能在windows操作系统上使用。选择OpenGL的一个很大的原因就是因为它的
跨平台性，所以我们可以尽量的使用glut库。